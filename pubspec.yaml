name: flutter_application_1
description: "SP!ED Android morning routine prototype."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.12.0+2
  cupertino_icons: ^1.0.8
  flutter_blue_plus: 2.3.12
  flutter_tts: 4.2.5
  geolocator: ^14.0.3
  http: ^1.6.0
  image: 4.9.2
  speech_to_text: 7.4.0
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/movenet_lightning.tflite
